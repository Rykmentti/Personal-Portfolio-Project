using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//Kaikkien vihollisten pit‰‰ inherittaa t‰‰ script, koita laittaa kaikkia vihollisia koskevat fieldit t‰h‰n. Kaikille sitten omat arvot Start:issa, omissa scripteiss‰‰n.
//Huomattavasti helpompaa tehd‰ multi-purpose/multi-object scriptej‰/koodia inheritancen avulla.
public class EnemyManager : MonoBehaviour
{
    public float enemyHealth;
    public float speed;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
